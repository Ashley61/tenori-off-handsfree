window.handsfree = new Handsfree({debug: true})

// Click on containers
handsfree.use({
  name: 'clickContainers',
  onFrame: faces => {
    faces.forEach(face => {
      if (face.cursor.
    })
  }
})